# Shutter Rank - Спецификация функционала

Этот документ описывает ключевые реализованные функции и требования к приложению Shutter Rank для обеспечения консистентности при тестировании и дальнейшей разработке.

## 1. Основное приложение (Голосование)

### 1.1. Управление сессиями
- Приложение поддерживает несколько независимых сессий голосования.
- Доступ к конкретной сессии осуществляется через хэш в URL (например, `index.html#session-name`).
- При отсутствии хэша пользователю отображается страница выбора из всех доступных сессий.
- В случае ошибки загрузки или отсутствия сессии отображается соответствующее сообщение.

### 1.2. Пользовательский интерфейс (UI)
- **Вступительная статья**: При первом входе в сессию отображается модальное окно с форматированной статьей (Markdown).
- **Два режима отображения ленты**:
    1.  **Сетка (Grid)**: Фотографии отображаются с фиксированными пропорциями (настраивается в `Settings`).
    2.  **Оригинальные пропорции (Original/Masonry)**: Фотографии отображаются с сохранением исходных пропорций в несколько колонок.
- **Адаптивность**: Интерфейс адаптирован для десктопных и мобильных устройств.
- **Настройки вида**: Пользователь может изменять режим отображения и пропорции сетки через модальное окно настроек. Выбор сохраняется в `localStorage`.
- **Кастомные диалоги**: Все системные сообщения (`confirm`) заменены на кастомные неблокирующие модальные окна, которые не прерывают полноэкранный режим.
- **Кнопка "Поделиться"**: В заголовке сессии доступна кнопка для копирования прямой ссылки на текущую сессию в буфер обмена.

### 1.3. Процесс голосования
- **Система рейтинга**: 5-звёздочная система оценки.
- **Лимиты голосования**:
    - Ограничение на количество оцененных фотографий (`ratedPhotoLimit`).
    - Ограничение на общую сумму потраченных звёзд (`totalStarsLimit`).
- **Индикаторы лимитов**: В основной и "липкой" шапке отображается статистика по использованным лимитам.
- **Сохранение прогресса**: Голоса пользователя (рейтинги) сохраняются в Firebase Realtime Database в реальном времени под анонимным `userId` (хранится в `localStorage`).
- **Сброс голосов**: Пользователь может сбросить все свои оценки для текущей сессии через кастомный диалог подтверждения.
- **Фото "вне конкурса"**: Такие фото имеют пунктирную обводку, и за них нельзя голосовать.

### 1.4. Динамический рейтинг (разблокировка 4-й и 5-й звезды)
- По умолчанию для оценки доступны только 1-3 звезды.
- 4-я и 5-я звезды визуально отличаются (пониженная прозрачность).
- Разблокировка 4-й и 5-й звезды происходит автоматически, если суммарный рейтинг фотографии (от всех пользователей) превышает средний рейтинг по сессии на определённый процент (`unlockFourStarsThresholdPercent`, `unlockFiveStarsThresholdPercent`).
- При попытке поставить "заблокированную" оценку появляется информационное модальное окно.

### 1.5. Группы фотографий ("Спойлер")
- **Новая парадигма**: Взаимодействие с группами реализовано через механику "спойлера" (аккордеона), который раскрывается прямо в ленте, не прерывая пользовательский опыт.
- **Сложенное состояние**:
    - В ленте группа отображается как одна карточка-стопка стандартного размера.
    - Под карточкой расположен "язычок" с названием группы и иконкой-стрелкой (🔽), который является единственной точкой входа для раскрытия.
- **Раскрытое состояние**:
    - По клику на "язычок" карточка-стопка скрывается, а на её месте плавно появляется контейнер, занимающий всю ширину ленты.
    - Внутри контейнера на специальной подложке отображаются все фотографии группы в виде стандартных карточек.
    - Вверху и внизу контейнера расположены кнопки "Свернуть группу" с иконкой-стрелкой (🔼).
- **Выбор и оценка (Парадигма "Интуитивный выбор")**:
    - **Выбор через оценку**: При установке оценки (звёзд) на любую фотографию в раскрытой группе, она **автоматически становится "выбранной"**.
    - **Прямой выбор**: Пользователь может выбрать фото, нажав на контрол (кружок), который после выбора превращается в зелёный кружок с галочкой.
    - **Перенос оценки**: Если пользователь пытается оценить или выбрать новое фото, когда в группе уже есть другое с оценкой, появляется **единый** кастомный диалог с предложением перенести оценку.
    - **Снятие оценки**: Если пользователь снимает оценку с выбранной фотографии, с неё **автоматически и без вопросов** снимается и статус "выбранной".
- **Состояние выбора**: Выбор пользователя для каждой группы сохраняется в `localStorage`.

### 1.6. Режимы просмотра (Modal / Immersive)
- **Сквозная навигация**: Листание в режимах просмотра происходит по **плоскому списку** всех видимых фотографий, включая все снимки из групп.
- **"Умные" рамки и виньетирование**:
    - Фото из группы, которое **выбрано**, имеет яркую **зелёную** рамку.
    - Остальные фото из той же группы имеют эффект **виньетирования** (затемнение по краям) и нейтральную серую рамку.
    - Фото из группы, в которой ещё ничего не выбрано, имеет нейтральную серую рамку (без виньетирования).
- **"Экспертный режим"**:
    - В режиме просмотра на фотографии из группы появляется кнопка "Перейти в группу".
    - **Только по нажатию на эту кнопку** открывается старое модальное окно, показывающее все фото из группы вместе для детального сравнения.

### 1.7. Система видимости фотографий
- **Метафора "Глаза"**: Вместо флажков используется иконка глаза для управления видимостью.
- **Скрытие**:
    - При наведении на фото появляется иконка открытого глаза (👁️).
    - Клик по иконке скрывает фотографию, которая плавно исчезает из ленты.
- **Запрет на скрытие**: Фотографии, которым выставлена оценка, **нельзя скрыть**. Иконка глаза на них отображается в неактивном (disabled) состоянии (только при наведении).
- **Глобальный фильтр**: В основной и "липкой" шапке есть переключатель "Показывать скрытые".
    - По умолчанию он выключен.
    - При включении в ленте появляются все скрытые фотографии, на которых постоянно отображается иконка перечёркнутого глаза (👁️‍🗨️).

## 2. Административные инструменты (Редактор)

### 2.1. Общее
- **Адрес**: `editor.html`.
- **Функции**: Создание, редактирование конфигурации, управление списком фото и группами.
- **Миграция**: Автоматически мигрирует сессии старого формата при открытии.
- **Интерфейс**: Используются сворачиваемые блоки (`<details>`) для компактного отображения настроек, статьи и групп.

### 2.2. Загрузка фотографий
- **Drag & Drop**: Поддерживается перетаскивание файлов прямо в браузер.
- **Cross-Project Upload**: Реализована прямая загрузка в Google Cloud Storage (через JSON API) для поддержки сценария, когда бакет находится в другом проекте Google Cloud, отличном от проекта Firebase.
- **Диагностика проблем**:
    - При ошибке загрузки (CORS) или ошибке доступа (AccessDenied) автоматически открывается окно помощи `StorageHelpModal`.
    - Окно предоставляет готовые консольные команды (`gcloud`) для настройки CORS и прав доступа (`roles/storage.objectViewer`).
- **Верификация**: После загрузки система автоматически проверяет доступность файла по публичной ссылке.

### 2.3. AI-функции
- **Генерация описаний**: Интеграция с Google Gemini API для автоматического создания описаний к фото.
- **Настройки**: API-ключ и системный промпт сохраняются локально в браузере пользователя (`localStorage`), не передаваясь на сервер.

### 2.4. Управление данными
- **Sanitization**: Перед сохранением в Firebase данные автоматически очищаются от полей со значением `undefined`, чтобы предотвратить ошибки базы данных.
- **Экспорт**: Возможность скачать готовую HTML-галерею результатов.
- **EXIF**: Возможность пакетного извлечения описаний из метаданных фотографий.

## 3. Техническая реализация

### 3.1. Инфраструктура
- **Frontend**: React 18, Vite.
- **Backend**: Firebase Realtime Database.
- **Storage**: Google Cloud Storage (выделенный бакет `shutter-rank-storage`).
- **Деплой**: GitHub Actions (каналы `dev` и `live`).

### 3.2. Безопасность
- База данных защищена правилами Firebase Security Rules (требуется валидация типов данных).
- Загрузка в Storage разрешена для `allUsers` (роль `Storage Object User`) для упрощения администрирования без сложной аутентификации, но требует настройки CORS.
- Просмотр файлов требует роли `Storage Object Viewer` для `allUsers`.
